# --- Secrets: never commit real .env files (any folder) ---
.env
**/.env
.env.*
# ...but DO keep the templates
!.env.example
!**/.env.example

# Logs
*.log

# Local verification artifacts
.verify/

# Local compose override (dev-only)
docker-compose.override.yml
