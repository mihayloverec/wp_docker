# Caddy edge proxy settings (copy to .env)

# Must be the SAME shared network name used by every site stack's .env.
PROXY_NETWORK=web-proxy

# Email for Let's Encrypt certificate notices.
ACME_EMAIL=you@example.com
